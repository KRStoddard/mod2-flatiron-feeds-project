<h1> <%= @group.name %>'s Members </h1>

<ul> <% @group.sort_memberships.each do |groupmember| %>
    <li> <%= link_to groupmember.user.full_name, user_path(groupmember.user) %>
    <ul>
        <li style="list-style: none"> Joined: <%= groupmember.created_at %> </li>
     </ul>
    </li>
<% end %>
</ul>


